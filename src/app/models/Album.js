class Album {
    constructor(albumID, name, imageURL, listSong) {
      this.albumID = albumID;
      this.name = name;
      this.imageURL = imageURL;
      this.listSong = listSong;
    }
}

module.exports = Album;